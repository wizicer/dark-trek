{
  "workspaces": ["ui", "circuits", "contracts"],
  "private": true,
  "scripts": {
    "build:circuits": "cd circuits && npm run build",
    "build:contracts": "cd contracts && npm run build",
    "build": "npm run build:circuits && npm run build:contracts"
  }
}